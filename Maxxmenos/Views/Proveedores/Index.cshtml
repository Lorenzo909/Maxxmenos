@model IEnumerable<Maxxmenos.Models.Proveedor>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<div class="col-md-8 mx-auto mt-2">
    <h3>Lista de Proveedores</h3>
    <a class="btn btn-secondary text-light mb-2" asp-action="Create">Create New</a>

    <table id="table_id" class="display mt-2">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Compania)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Representante)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Telefono)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Compania)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Representante)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Telefono)
                    </td>
                    <td>
                        <a asp-action="Edit" asp-route-id="@item.IdProveedor">Edit</a> |
                        <a asp-action="Details" asp-route-id="@item.IdProveedor">Details</a> |
                        <a asp-action="Delete" asp-route-id="@item.IdProveedor">Delete</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>

    @section scripts{
        <script>
            $('#table_id').DataTable({
                "processing": true,
                "searching": false

            });
        </script>
    }
